class AbstractRequests {
    get(app) {
    }

    post(app) {
    }

    put(app) {
    }

    delete(app) {
    }

    methods(app) {
        this.get(app)
        this.post(app)
        this.put(app)
        this.delete(app)
    }
}

module.exports = AbstractRequests
